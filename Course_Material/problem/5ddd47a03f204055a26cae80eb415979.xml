<problem display_name="Problem 4" markdown="null" showanswer="never">
  <p>For this problem, the graders will use our implementation of the <code>Message</code>, <code>PlaintextMessage</code>, and <code>CiphertextMessage</code> classes, so don't worry if you did not get the previous parts correct.</p>
  <p>Now that you have all the pieces to the puzzle, please use them to decode the file story.txt. The file ps6.py contains a helper function <code>get_story_string()</code> that returns
  the encrypted version of the story as a string. Create a <code>CiphertextMessage</code> object
  using the story string and use <code>decrypt_message</code> to return the appropriate shift
  value and unencrypted story string.</p>
  <p>Paste your function <code>decrypt_story()</code> in the box below.</p>
  <coderesponse queuename="Watcher-MITx-6.00x">
    <textbox rows="30" cols="70" mode="python" tabsize="4"/>
    <codeparam>
      <initial_display>
</initial_display>
      <grader_payload>
{"grader": "ps06/caesar4/grade_decrypt_story.py"}
</grader_payload>
    </codeparam>
  </coderesponse>
</problem>
