<problem>
  <text>
    <p>
      Write a Python function that returns a list of keys in <code>aDict</code> with the value
      <code>target</code>. The list of keys you return should be sorted in increasing order. The keys and values in <code>aDict</code> are both integers. (If <code>aDict</code> does not
      contain the value <code>target</code>, you should return an empty list.)</p>
    <p>This function takes in a dictionary and an integer and returns a
  list.</p>
    <pre>def keysWithValue(aDict, target):
    '''
    aDict: a dictionary
    target: an integer
    '''
    # Your code here  </pre>
  </text>
  <coderesponse queuename="Watcher-MITx-6.00x">
    <textbox rows="30" cols="80" mode="python" tabsize="4"/>
    <codeparam>
      <initial_display># Paste your function here</initial_display>
      <answer_display>
def keysWithValue(aDict, target):
    targetKeys = []
    for i in aDict.keys():
        if aDict[i] == target:
            targetKeys.append(i)
    targetKeys.sort()
    return targetKeys
</answer_display>
      <grader_payload>
        {"grader": "finger_exercises/L6/keys_with_value/grade_keys_with_value.py"}
</grader_payload>
    </codeparam>
  </coderesponse>
</problem>
